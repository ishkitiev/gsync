# gsync
Safe interactive rsync wrapper for LAN mirror backups. `gsync` is a lightweight Bash wrapper around `rsync`, designed for safe and convenient offline mirror backups between Linux machines over LAN.
